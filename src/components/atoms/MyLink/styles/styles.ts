import { css } from "@emotion/css";
import fontWeights from "../../../../shared/constants/design-system/font-weights";

export const myLinkStyle = css`
  font-weight: ${fontWeights.bold};
  text-decoration: underline;
  cursor: pointer;
  display: inline-block;
`